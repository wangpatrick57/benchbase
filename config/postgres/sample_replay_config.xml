<?xml version="1.0"?>
<parameters>

    <!-- Connection details -->
    <type>POSTGRES</type>
    <driver>org.postgresql.Driver</driver>
    <url>jdbc:postgresql://localhost:5432/benchbase?sslmode=disable&amp;ApplicationName=replay&amp;reWriteBatchedInserts=true</url>
    <username>admin</username>
    <password>password</password>
    <isolation>TRANSACTION_SERIALIZABLE</isolation>
    <batchsize>128</batchsize>

    <!-- The workload -->
    <!-- Terminals controls the # of workers. Increase this if the system cannot keep up with the replay file -->
    <terminals>2</terminals>
    <works>
        <work>
            <time>1</time>
            <rate>1</rate>
            <weights>1</weights>
        </work>
    </works>

    <!-- Transaction types -->
    <transactiontypes>
        <transactiontype>
            <name>DynamicProcedure</name>
        </transactiontype>
    </transactiontypes>
    
</parameters>